import { SortTermsPipe } from './sort-terms.pipe';

describe('SortTermsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTermsPipe();
    expect(pipe).toBeTruthy();
  });
});
